{
  "name": "@tstwo/app",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "bun run index.ts",
    "build": "vite build",
    "test": "bun test --coverage",
    "lint": "eslint . --max-warnings=0"
  },
  "dependencies": {
    "@tstwo/core": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
} 