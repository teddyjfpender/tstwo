{
  "name": "tstwo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "prebuild": "bun link --all",
    "build": "bun run -F=./packages/* build",
    "dev": "bun run -F=./packages/* dev",
    "test": "bun run -F=./packages/* test",
    "lint": "bun run -F=./packages/* lint"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
